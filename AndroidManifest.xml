<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uk.ac.ed.informatics_forum"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="10" />
    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" 
        android:theme="@android:style/Theme.NoTitleBar" >
        <activity
            android:label="@string/app_name"
            android:name=".activities.Index" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.Directions"></activity>
        <activity android:name=".activities.FoodAndDrink"></activity>
        <activity android:name=".activities.Hotels"></activity>
        <activity android:name=".activities.PracticalInformation"></activity>
        <activity android:name=".activities.Planner"></activity>
    </application>

</manifest>